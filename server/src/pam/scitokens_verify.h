#ifndef _SVERIFY_H_
#define _SVERIFY_H_

/*
 * System includes.
 */
#include <stdlib.h>
#include <string.h>

int
scitoken_verify(const char * auth_line, const struct config * config);

#endif /* _SVERIFY_H_ */
